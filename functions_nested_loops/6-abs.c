#include "main.h"

/**
* _abs- aide pour la conduite pour le systeme de frein
* @a: le a initie les chiffres
* Return: Always 0 (Success)
*/

#include "main.h"

int _abs(int a)
{
	if (a > 0)
	{
	return (a);
	}
	else if (a < 0)
	{
	return (-a);
	}
return (0);
}
