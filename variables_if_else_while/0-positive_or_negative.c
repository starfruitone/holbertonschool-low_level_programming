#include <stdio.h>

int main(void)
{
    int n = 1;
    if (n > 0)
        printf("0 is positive\n");
    else if (n == 0)
        printf("0 is zero\n");
    else
        printf("0 is negative\n");
    return (0);
}
